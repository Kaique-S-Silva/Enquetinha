services:
  bot:
    build: .
    container_name: enquetinha
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - DB_PATH=data/enqutinha_prod.db
    volumes:
      - ./data:/app/data